# ── Streamlit 웹 앱 (app.py) ──────────────────────────────────────────────────
streamlit>=1.32.0
httpx==0.27.0
pandas>=2.0.0
openpyxl>=3.1.0
python-dotenv>=1.0.0

# ── 크롤러 스크립트 (로컬 실행용, Streamlit Cloud에서는 불필요) ──────────────
# playwright>=1.44.0   # 설치 후 `playwright install chromium` 필요
# supabase>=2.4.0
